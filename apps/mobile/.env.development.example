# Copy to apps/mobile/.env.development (gitignored) for Metro.
# EAS production / development profiles stay on https://hop-uokqmg.fly.dev — do not change eas.json for local work.
# Physical iPhone: replace with `ipconfig getifaddr en0`
EXPO_PUBLIC_API_URL=http://127.0.0.1:8000
