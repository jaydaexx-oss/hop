# Copy to apps/mobile/.env (gitignored). Restart Metro / rebuild JS after changing.
# Do not use Expo Go. Native BLE requires a development client (`expo-dev-client`).
# This file has no secrets — only the public API origin.

# Production HOP API on Fly (physical iPhone / live HTTPS). App: hop-uokqmg (never hop-api).
EXPO_PUBLIC_API_URL=https://hop-uokqmg.fly.dev

# iOS Simulator / this Mac (loopback is this computer):
# EXPO_PUBLIC_API_URL=http://127.0.0.1:8000

# Physical iPhone talking to a Mac LAN API instead of production.
# Localhost on the phone is the phone itself and cannot reach your Mac.
# Find the Mac LAN IP:  ipconfig getifaddr en0
# Then set (example):
# EXPO_PUBLIC_API_URL=http://192.168.1.23:8000
#
# Required to clear “Too many new accounts from this network” while testing:
# production hop-uokqmg keeps the 5/IP/24h limiter (reset endpoint 404s).
# After pointing at the LAN API, restart Metro and use the hidden diagnostics
# reset on the phone. Mac curl to localhost cannot clear the iPhone’s IP.
#
# The API must bind all interfaces:  uvicorn ... --host 0.0.0.0 --port 8000
# Development builds allow cleartext HTTP only to loopback and RFC1918 LAN IPs.
# iOS ATS local-networking is enabled for that LAN HTTP path.
# Release builds refuse LAN cleartext — production must stay HTTPS.

# Android emulator (host machine from the AVD):
# EXPO_PUBLIC_API_URL=http://10.0.2.2:8000
