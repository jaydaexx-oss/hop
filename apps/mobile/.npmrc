# Keep the same flattened node_modules layout locally and on EAS.
# Do not introduce a repo-root npm workspace: EAS resolves the package
# manager from apps/mobile, and the lockfile must live next to this app.
install-strategy=hoisted
package-lock=true
