import { PlaceholderScreen } from '@/components/PlaceholderScreen';

export default function ContactsScreen() {
  return (
    <PlaceholderScreen
      title="Contacts"
      body="Contacts are not implemented. HOP will not upload an address book without explicit consent."
    />
  );
}
