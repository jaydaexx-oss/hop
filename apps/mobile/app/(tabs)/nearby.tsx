import { PlaceholderScreen } from '@/components/PlaceholderScreen';

export default function NearbyScreen() {
  return (
    <PlaceholderScreen
      title="Nearby"
      body="Bluetooth discovery is not implemented. This screen will list nearby HOP users by display name only — never MAC addresses or GPS."
    />
  );
}
